# 10K静态动作描述数据集 - 使用指南

## 📊 数据集概览

**文件**: `10K静态动作描述.md`  
**总数**: 10,000条唯一的静态动作描述  
**文件大小**: 842 KB  
**生成时间**: 2025年12月2日

---

## 📈 数据集统计

### 关键词分布

| 关键词 | 数量 | 占比 | 说明 |
|--------|------|------|------|
| arms | 4,720 | 47.2% | 手臂相关动作 |
| legs | 3,552 | 35.5% | 腿部相关动作 |
| raises | 2,933 | 29.3% | 抬起/举起动作 |
| standing | 2,708 | 27.1% | 站立姿态 |
| leaning | 2,229 | 22.3% | 倾斜姿态 |
| stretches | 1,839 | 18.4% | 伸展动作 |
| sitting | 1,210 | 12.1% | 坐姿 |
| twisting | 1,056 | 10.6% | 扭转动作 |
| bending | 502 | 5.0% | 弯曲动作 |
| hands | 362 | 3.6% | 手部动作 |
| kneeling | 305 | 3.0% | 跪姿 |
| squatting | 183 | 1.8% | 蹲姿 |

### 描述长度

- **最短**: 19 字符
- **最长**: 110 字符
- **平均**: 79.2 字符

### 动作类型分布

- **站立姿态**: ~27%
- **坐姿**: ~12%
- **蹲姿**: ~2%
- **跪姿**: ~3%
- **复合动作**: ~56%

---

## 🎯 使用方法

### 方法1: 单个生成

从文件中选择一条描述，直接生成：

```bash
python sample_single_pose.py \
    --text_prompt "A person raises both arms above the head" \
    --dataset_name t2m \
    --save_image \
    --save_json
```

### 方法2: 批量生成（推荐）

#### 步骤1: 从数据集中选择描述

```bash
# 提取前100条描述到txt文件
sed -n '28,127p' 10K静态动作描述.md | sed 's/^[0-9]*\. //' > batch_100.txt

# 或者随机选择100条
grep "^[0-9]*\." 10K静态动作描述.md | shuf -n 100 | sed 's/^[0-9]*\. //' > random_100.txt
```

#### 步骤2: 批量生成

```bash
python sample_single_pose.py \
    --text_path batch_100.txt \
    --dataset_name t2m \
    --repeat_times 3 \
    --save_image \
    --save_json
```

### 方法3: 按类别筛选

#### 筛选站立姿态

```bash
grep "standing" 10K静态动作描述.md | sed 's/^[0-9]*\. //' > standing_poses.txt
```

#### 筛选坐姿

```bash
grep "sitting" 10K静态动作描述.md | sed 's/^[0-9]*\. //' > sitting_poses.txt
```

#### 筛选举手动作

```bash
grep "raises.*arms" 10K静态动作描述.md | sed 's/^[0-9]*\. //' > raising_arms_poses.txt
```

#### 筛选瑜伽姿势

```bash
grep "pose" 10K静态动作描述.md | sed 's/^[0-9]*\. //' > yoga_poses.txt
```

---

## 💡 实用脚本

### 脚本1: 随机生成N个姿态

```bash
#!/bin/bash
# random_generate.sh - 随机生成指定数量的姿态

NUM=${1:-10}  # 默认10个

echo "随机选择 $NUM 条描述..."
grep "^[0-9]*\." 10K静态动作描述.md | \
    shuf -n $NUM | \
    sed 's/^[0-9]*\. //' > temp_random.txt

echo "开始生成姿态..."
python sample_single_pose.py \
    --text_path temp_random.txt \
    --dataset_name t2m \
    --repeat_times 3 \
    --save_image \
    --save_json

rm temp_random.txt
echo "完成！"
```

使用：
```bash
chmod +x random_generate.sh
./random_generate.sh 50  # 生成50个随机姿态
```

### 脚本2: 按范围生成

```bash
#!/bin/bash
# range_generate.sh - 生成指定范围的姿态

START=${1:-1}
END=${2:-100}

echo "提取描述 $START 到 $END..."
START_LINE=$((START + 27))  # 跳过文件头
END_LINE=$((END + 27))

sed -n "${START_LINE},${END_LINE}p" 10K静态动作描述.md | \
    sed 's/^[0-9]*\. //' > temp_range.txt

echo "开始生成姿态..."
python sample_single_pose.py \
    --text_path temp_range.txt \
    --dataset_name t2m \
    --repeat_times 2 \
    --save_image

rm temp_range.txt
echo "完成！"
```

使用：
```bash
chmod +x range_generate.sh
./range_generate.sh 1 100    # 生成第1-100条
./range_generate.sh 101 200  # 生成第101-200条
```

### 脚本3: 分类统计

```bash
#!/bin/bash
# analyze_dataset.sh - 分析数据集

echo "=== 10K静态动作描述数据集分析 ==="
echo ""

echo "总描述数:"
grep "^[0-9]*\." 10K静态动作描述.md | wc -l

echo ""
echo "按类型统计:"
echo "  站立姿态: $(grep -c "standing" 10K静态动作描述.md)"
echo "  坐姿: $(grep -c "sitting" 10K静态动作描述.md)"
echo "  蹲姿: $(grep -c "squatting" 10K静态动作描述.md)"
echo "  跪姿: $(grep -c "kneeling" 10K静态动作描述.md)"

echo ""
echo "按动作部位统计:"
echo "  手臂动作: $(grep -c "arms" 10K静态动作描述.md)"
echo "  手部动作: $(grep -c "hands" 10K静态动作描述.md)"
echo "  腿部动作: $(grep -c "legs" 10K静态动作描述.md)"

echo ""
echo "按动作类型统计:"
echo "  举起动作: $(grep -c "raises" 10K静态动作描述.md)"
echo "  伸展动作: $(grep -c "stretches" 10K静态动作描述.md)"
echo "  弯曲动作: $(grep -c "bending" 10K静态动作描述.md)"
echo "  扭转动作: $(grep -c "twisting" 10K静态动作描述.md)"
```

---

## 🎨 示例场景

### 场景1: 构建姿态数据库

生成1000个不同的静态姿态：

```bash
# 每次100个，分10批
for i in {1..10}; do
    start=$((($i-1)*100 + 1))
    end=$(($i*100))
    echo "生成第 $start-$end 个姿态..."
    
    sed -n "$((start+27)),$((end+27))p" 10K静态动作描述.md | \
        sed 's/^[0-9]*\. //' > batch_${i}.txt
    
    python sample_single_pose.py \
        --text_path batch_${i}.txt \
        --dataset_name t2m \
        --repeat_times 1 \
        --save_image \
        --sequence_length 8  # 快速模式
    
    rm batch_${i}.txt
done
```

### 场景2: 生成特定类型姿态集

```bash
# 生成所有站立姿态
grep "standing" 10K静态动作描述.md | \
    sed 's/^[0-9]*\. //' > standing_all.txt

python sample_single_pose.py \
    --text_path standing_all.txt \
    --dataset_name t2m \
    --repeat_times 2 \
    --save_image
```

### 场景3: 质量筛选

生成多个变体，手动筛选最好的：

```bash
# 选择50个描述，每个生成5个变体
grep "^[0-9]*\." 10K静态动作描述.md | \
    shuf -n 50 | \
    sed 's/^[0-9]*\. //' > quality_test.txt

python sample_single_pose.py \
    --text_path quality_test.txt \
    --dataset_name t2m \
    --repeat_times 5 \
    --save_image \
    --save_json
```

### 场景4: 快速预览

快速浏览数据集效果：

```bash
# 随机选10个，快速生成
grep "^[0-9]*\." 10K静态动作描述.md | \
    shuf -n 10 | \
    sed 's/^[0-9]*\. //' > preview.txt

python sample_single_pose.py \
    --text_path preview.txt \
    --dataset_name t2m \
    --repeat_times 1 \
    --save_image \
    --sequence_length 8  # 快速模式
```

---

## 📋 数据集内容示例

### 基础站立姿态
```
A person standing upright
A person standing naturally
A person standing with feet apart
A person standing with feet together
A person standing on tiptoes
```

### 手臂动作
```
A person raises both arms above the head
A person stretches arms forward
A person holds arms out horizontally
A person crosses arms over chest
A person extends arms diagonally upward
```

### 坐姿
```
A person sitting on a chair
A person sitting cross-legged
A person sitting with legs extended
A person sitting in lotus position
A person sitting on the ground
```

### 复合动作
```
A person standing and raises both arms above the head
A person sitting cross-legged and points both hands forward
A person leaning forward with stretches arms to the side
A person twisting torso to the right with holds arms at chest level
```

### 瑜伽/健身姿势
```
A person in tree pose
A person in warrior pose
A person in triangle pose
A person in plank position
A person in bridge pose
```

---

## 🔍 数据质量说明

### 优点

✅ **多样性高**: 10,000条唯一描述，覆盖多种姿态类型  
✅ **结构化**: 遵循一致的语法结构  
✅ **可组合**: 包含单一动作和复合动作  
✅ **易筛选**: 可按关键词快速筛选  
✅ **即用**: 可直接用于生成姿态

### 注意事项

⚠️ **生成质量**: 不同描述的生成质量可能有差异  
⚠️ **文本准确性**: 需要测试验证描述是否准确对应姿态  
⚠️ **复杂度**: 复合动作可能比单一动作更难生成准确  

### 建议

1. **先小批量测试**: 从每类选几个测试效果
2. **使用repeat_times**: 生成多个变体供选择
3. **调整参数**: 根据需要调整cfg、time_steps等
4. **质量筛选**: 生成后人工筛选质量好的

---

## 📊 推荐的批量生成策略

### 策略1: 分层采样

```bash
# 从每个类别均匀采样
grep "standing" 10K静态动作描述.md | shuf -n 200 > standing_sample.txt
grep "sitting" 10K静态动作描述.md | shuf -n 100 > sitting_sample.txt
grep "squatting" 10K静态动作描述.md | shuf -n 50 > squatting_sample.txt
grep "kneeling" 10K静态动作描述.md | shuf -n 50 > kneeling_sample.txt

# 合并
cat standing_sample.txt sitting_sample.txt squatting_sample.txt kneeling_sample.txt > balanced_400.txt

# 生成
python sample_single_pose.py --text_path balanced_400.txt --repeat_times 2 --save_image
```

### 策略2: 渐进式生成

```bash
# 第1轮: 快速生成所有（预览）
python sample_single_pose.py \
    --text_path all_10k.txt \
    --sequence_length 8 \
    --repeat_times 1

# 第2轮: 筛选后高质量生成
# (手动筛选出好的描述)
python sample_single_pose.py \
    --text_path selected_500.txt \
    --sequence_length 16 \
    --repeat_times 5 \
    --save_image \
    --save_json
```

### 策略3: 并行生成

```bash
# 分成10个文件并行生成
split -l 1000 all_10k.txt batch_

# 在不同GPU上并行运行
CUDA_VISIBLE_DEVICES=0 python sample_single_pose.py --text_path batch_aa &
CUDA_VISIBLE_DEVICES=1 python sample_single_pose.py --text_path batch_ab &
# ... 更多
```

---

## 🛠️ 工具脚本集合

### 创建所有实用脚本

```bash
# 保存为 create_tools.sh
cat > random_generate.sh << 'SCRIPT'
#!/bin/bash
NUM=${1:-10}
grep "^[0-9]*\." 10K静态动作描述.md | shuf -n $NUM | sed 's/^[0-9]*\. //' > temp_random.txt
python sample_single_pose.py --text_path temp_random.txt --dataset_name t2m --repeat_times 3 --save_image --save_json
rm temp_random.txt
SCRIPT

cat > extract_category.sh << 'SCRIPT'
#!/bin/bash
KEYWORD=${1:-standing}
OUTPUT=${2:-${KEYWORD}_poses.txt}
grep "$KEYWORD" 10K静态动作描述.md | sed 's/^[0-9]*\. //' > $OUTPUT
echo "提取了 $(wc -l < $OUTPUT) 条包含 '$KEYWORD' 的描述到 $OUTPUT"
SCRIPT

cat > batch_generate.sh << 'SCRIPT'
#!/bin/bash
INPUT=${1:-poses.txt}
REPEAT=${2:-3}
python sample_single_pose.py --text_path $INPUT --dataset_name t2m --repeat_times $REPEAT --save_image --save_json
SCRIPT

chmod +x random_generate.sh extract_category.sh batch_generate.sh
echo "✅ 工具脚本创建完成！"
```

---

## 📚 相关文档

- `10K静态动作描述.md` - 完整数据集
- `单帧姿态生成使用说明.md` - 生成脚本详细教程
- `单帧姿态生成-快速参考.md` - 快速参考卡
- `单帧姿态生成-测试结果.md` - 测试报告

---

## 🎯 快速开始

```bash
# 1. 随机生成10个姿态
grep "^[0-9]*\." 10K静态动作描述.md | shuf -n 10 | sed 's/^[0-9]*\. //' > test_10.txt
python sample_single_pose.py --text_path test_10.txt --save_image

# 2. 生成所有站立姿态
grep "standing" 10K静态动作描述.md | sed 's/^[0-9]*\. //' > standing.txt
python sample_single_pose.py --text_path standing.txt --repeat_times 2 --save_image

# 3. 批量生成前100个
sed -n '28,127p' 10K静态动作描述.md | sed 's/^[0-9]*\. //' > first_100.txt
python sample_single_pose.py --text_path first_100.txt --save_image
```

---

**数据集版本**: 1.0  
**最后更新**: 2025年12月2日  
**许可**: 用于研究和开发

